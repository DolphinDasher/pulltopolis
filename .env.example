# Server-only GitHub token. Do not use a query parameter or expose it to Vite.
GITHUB_TOKEN=

HOST=127.0.0.1
PORT=3000
DATABASE_PATH=data/pulltopolis.sqlite

GITHUB_REQUEST_TIMEOUT_MS=10000
GITHUB_RATE_LIMIT_RESERVE=100
